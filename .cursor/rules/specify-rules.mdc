# tg-say-my-post Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-10

## Active Technologies
- Python 3.11 (per project `requires-python >=3.11`). + torch + silero-models (via torch hub or predownloaded files), python-telegram-bot, pydub, pytest/pytest-asyncio. (002-silero-tts)
- Local filesystem for generated audio; no persistent DB. (002-silero-tts)
- Python 3.11 (uv-managed) + aiogram/pyrogram stack for bot, existing Silero TTS client, планируемый Piper TTS клиент + CLI обёртка, logging/metrics (стандартный logging + структурированные записи), pytest для тестов (004-piper-tts)
- Локальные файлы весов (кеш), конфиг из env; БД не затрагивается (004-piper-tts)
- Python 3.11 (uv-managed) + vosk-tts (pip), существующие aiogram/pyrogram для бота, pytest для тестов (005-vosk-tts)
- Локальные файлы моделей в кеше (скачиваются автоматически библиотекой vosk-tts) (005-vosk-tts)
- Python 3.11 (uv-managed) + python-telegram-bot, pydub (для mp3 handling), pytest (001-telegram-tts-bot)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11 (uv-managed): Follow standard conventions

## Recent Changes
- 005-vosk-tts: Added Python 3.11 (uv-managed) + vosk-tts (pip), существующие aiogram/pyrogram для бота, pytest для тестов
- 004-piper-tts: Added Python 3.11 (uv-managed) + aiogram/pyrogram stack for bot, existing Silero TTS client, планируемый Piper TTS клиент + CLI обёртка, logging/metrics (стандартный logging + структурированные записи), pytest для тестов
- 003-melotts-tts: Added Python 3.11 + uv (runtime/packaging), Telegram bot library (aiogram/pyrogram already in repo), existing Silero TTS client; добавить MeloTTS клиент.


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
